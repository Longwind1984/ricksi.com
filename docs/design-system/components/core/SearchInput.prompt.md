Capsule glass search input — used for knowledge-graph search and the site's command-palette trigger. Mono placeholder, accent focus ring via border, optional accent hint on the right.

```jsx
<SearchInput placeholder="搜索 141 篇笔记…" hint="12 篇" value={q} onChange={(e) => setQ(e.target.value)} />
```
