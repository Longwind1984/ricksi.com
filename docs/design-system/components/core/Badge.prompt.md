Capsule badges on the 人机光谱 system — two rules carry all meaning: **色温 = 人↔机** (provenance slides warm→cold as the human hand recedes: 实测·金 → 共创·青 → AI 整理·紫) and **虚线 = 未验证** (sample data and WIP repos get dashed borders; solid = verified).

```jsx
<Badge>多智能体</Badge>
<Badge variant="prov-human">实测</Badge>
<Badge variant="prov-co">共创</Badge>
<Badge variant="prov-ai">AI 整理</Badge>
<Badge variant="sample" />
<Badge variant="wip" />
```

The sample badge is a hard rule of this brand: missing real data must be visibly flagged, never faked.
