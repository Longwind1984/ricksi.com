name: Refresh GitHub contribution data

on:
  schedule:
    - cron: '17 22 * * *' # 每日北京时间 06:17
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Fetch GitHub contributions
        env:
          # 公开贡献日历优先用 PAT（secrets.CONTRIB_TOKEN）；没有则尝试内置 token
          GITHUB_TOKEN: ${{ secrets.CONTRIB_TOKEN || secrets.GITHUB_TOKEN }}
        run: node scripts/fetch-github.mjs
      - name: Commit & push if changed
        run: |
          git config user.name "data-bot"
          git config user.email "actions@github.com"
          # 先 commit 再 pull --rebase 再 push：fetch-github 写了 data/ 后工作树有未暂存改动，
          # 若先 pull --rebase 会报「cannot pull with rebase: You have unstaged changes」（曾 8/8 必挂）。
          if [[ -n "$(git status --porcelain data)" ]]; then
            git add data
            git commit -m "chore(data): github contributions $(date +%F)"
            git pull --rebase
            git push
          else
            echo "no changes"
          fi
